/**
 * MCP NAMING — jO(), rv(), jh(), Ao1() in cli.js
 *
 * See client.ts for evidence. This module handles the mcp__<server>__<tool>
 * naming convention. Extracted here since it's used by multiple systems
 * (tool registry, permissions, UI rendering).
 */

// See evidence in client.ts — same functions duplicated here for clarity
